import { DisableCategoriesPipe } from './disable-categories.pipe';

describe('DisableCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new DisableCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
