import { TranslateLanguagePipe } from './translate-language.pipe';

describe('TranslateLanguage', () => {
  it('create an instance', () => {
    const pipe = TranslateLanguagePipe;
    expect(pipe).toBeTruthy();
  });
});
