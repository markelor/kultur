import { TranslateCountPipe } from './translate-count.pipe';

describe('TranslateCount', () => {
  it('create an instance', () => {
    const pipe = TranslateCountPipe;
    expect(pipe).toBeTruthy();
  });
});
