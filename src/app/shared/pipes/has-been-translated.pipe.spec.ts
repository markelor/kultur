import { HasBeenTranslatedPipe } from './has-been-translated.pipe';

describe('HasBeenTranslatedPipe', () => {
  it('create an instance', () => {
    const pipe = new HasBeenTranslatedPipe();
    expect(pipe).toBeTruthy();
  });
});
