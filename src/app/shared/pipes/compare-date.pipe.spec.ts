import { CompareDatePipe } from './compare-date.pipe';

describe('CompareDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CompareDatePipe();
    expect(pipe).toBeTruthy();
  });
});
