import { ImageMarginPipe } from './image-margin.pipe';

describe('ImageMarginPipe', () => {
  it('create an instance', () => {
    const pipe = ImageMarginPipe;
    expect(pipe).toBeTruthy();
  });
});
