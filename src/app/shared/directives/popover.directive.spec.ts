import { PopoverDirective } from './popover.directive';

describe('PopoverDirective', () => {
  it('should create an instance', () => {
    const directive = PopoverDirective;
    expect(directive).toBeTruthy();
  });
});
