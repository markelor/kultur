import { DatepickerDirective } from './datepicker.directive';

describe('DatepickerDirective', () => {
  it('should create an instance', () => {
    const directive = DatepickerDirective;
    expect(directive).toBeTruthy();
  });
});
