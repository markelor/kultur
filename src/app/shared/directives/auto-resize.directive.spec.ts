import { AutoResizeDirective } from './auto-resize.directive';

describe('AutoResizeDirective', () => {
  it('should create an instance', () => {
    const directive = AutoResizeDirective;
    expect(directive).toBeTruthy();
  });
});
