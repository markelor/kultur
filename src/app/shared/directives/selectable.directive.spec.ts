import { SelectableDirective } from './selectable.directive';

describe('SelectableDirective', () => {
  it('should create an instance', () => {
    const directive = SelectableDirective;
    expect(directive).toBeTruthy();
  });
});
