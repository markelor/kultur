import { AutoHeightDirective } from './auto-height.directive';

describe('AutoHeightDirective', () => {
  it('should create an instance', () => {
    const directive = AutoHeightDirective;
    expect(directive).toBeTruthy();
  });
});
