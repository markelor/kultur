import { AutoCompleteDirective } from './auto-complete.directive';

describe('AutoCompleteDirective', () => {
  it('should create an instance', () => {
    const directive = AutoCompleteDirective;
    expect(directive).toBeTruthy();
  });
});
